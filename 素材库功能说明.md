# 素材库功能说明（重构契约）

> 本文档是「素材库重构」的功能契约。重构目标：**纯前端、所有功能保留、渲染健壮（加载/空/错三态 + 计数）**。
> 凡本文列出的功能，重构后必须一一对应、不可丢失。

---

## 1. 概述

剧情编辑器是一套**纯前端**的单页应用（SPA），双击本地 `index.html` 即可运行，**不依赖任何本地服务器**。

左侧为「素材库」（五库 + 生成器 + 备份），右侧为剧情文字编辑区与预览。素材库的所有素材通过**拖拽（横屏）或点按（竖屏）**插入到剧情中，生成对应的召唤指令。

存储后端：
- **素材二进制（图片 / 音频 / GLB）** → IndexedDB（容量大，支持 `file://`）。
- **剧情结构 / 剧情块 / 元数据** → localStorage（体积小）。
- 每个**项目**拥有独立命名空间，互不可见、互不污染。

---

## 2. 五大素材库

| 库 ID | 名称 | 卡缩略图 | 存储字段 | 召唤指令 |
|-------|------|----------|----------|----------|
| `background` | 背景库 | 图片预览 | `id, name, kind('image'\|'gradient'\|'noise'), src(dataURL)` | `[召唤背景:名]` |
| `item` | 物品库 | 3D 角标（无图时用占位） | `id, name, glb(dataURL), exitMesh, interactions, sounds, defaultView, bg` | `[召唤物品:名]` |
| `music` | 音乐库 | 音频波形/图标 | `id, name, src(dataURL)` | `[召唤音乐:名]` |
| `sound` | 音效库 | 音频图标 | `id, name, src(dataURL)` | `[召唤音效:名]` |
| `dialogueblock` | 剧情块库 | 文字块 | 见 §7 | `<剧情块:名>` / `<选项:...>` |

> 注：`dialogueblock` 不是「二进制素材」，而是基于 localStorage 的剧情文本块仓库（见 §7）。它和「停止音乐」功能块（§8）是用户在早期 bug 中仍能正常看到的部分，重构时务必保留其渲染路径。

---

## 3. 素材卡通用交互

所有素材卡（`makeCard`）统一提供以下能力：

1. **删除** — 删除当前素材（带二次确认）。
2. **重命名** — 修改展示名（不影响内部 `id`）。
3. **试听 / 预览**（音频类）— 音乐 / 音效卡可点播放；物品卡可预览 3D。
4. **插入指令**：
   - **横屏（pointer 宽裕）**：按住卡片**拖拽**到右侧文字区，落点处插入对应 `[召唤X:名]` 指令，光标定位到指令之后。
   - **竖屏（窄窗 / 移动端）**：**点按**卡片即插入指令（避免拖拽不便）。
   - 物品卡拖入时自动附加文字提示参数（如 `[召唤物品:名|提示文字]`），并把光标移到提示文字处便于编辑。
5. **计数**：每个库顶部显示「共 N 项」。

---

## 4. 导入方式

### 4.1 背景图导入
- 入口：`背景库` → 「上传背景」按钮 / 库面板拖入图片。
- 自动处理：缩放到 **高 ≤ 720px**，颜色量化 **16 色（median-cut）**，导出为 **JPG**（减小体积，避免撑爆存储）。
- 支持格式：常见图片（png/jpg/webp 等）。

### 4.2 物品（GLB）导入
- 入口：`物品库` → 「上传物品」/ 拖入 `.glb`。
- 存入 `glb` 字段（base64 dataURL），并记录可选 `exitMesh`（结束物体）、`interactions`、`sounds`、`defaultView`、`bg`。

### 4.3 音频导入（自动归类）
- 入口：`音乐库` / `音效库` 上传按钮，或库面板拖入音频。
- **自动归类规则**（按音频时长）：
  - `< 10s` → 归入**音效库**。
  - `> 30s` → 归入**音乐库**。
  - `10s ~ 30s` → 弹窗询问用户选音乐还是音效。
  - 取不到时长时 → 归入**当前所在库**（即用户是在哪个库点上传的）。

### 4.4 场景包（GLB Bundle）导入
- 入口：`物品库` → 「导入场景包」/ 拖入 `.json`（schema = `glb-scene-bundle`）。
- 行为：把包里**每个模型拆成「一个可召唤物品」**，逐个写入物品库（保留 `exitMesh / interactions / sounds / defaultView / bg`）。
- 来源：由 3D交互制作器（`exportSceneBundle`）导出。

### 4.5 拖拽落点
- 素材库面板本身是一个**拖放接收区**：把外部文件拖到库面板上也能触发对应导入（按文件类型路由到背景/物品/音频/场景包）。

---

## 5. 背景生成器

两个内置生成器，产物作为**背景素材**存入背景库：

1. **渐变生成器**（`openGradientGen`）
   - 可选多色标、角度，实时预览，生成 CSS 渐变背景图（导出为 dataURL 存入背景库）。
2. **噪波生成器**（`openNoiseGen`）
   - 可调参数（密度 / 颜色 / 尺寸），用 Canvas 生成噪波纹理，导出为 dataURL 存入背景库。
   - ⚠️ 历史 bug：噪波生成曾因走 localStorage 存大图触发 `QuotaExceededError`。重构后必须走 IndexedDB，确保生成成功。

---

## 6. GLB 物品召唤与 3D 界面

- 游戏运行时遇到 `[召唤物品:名]`，以**全屏覆盖弹层（iframe）**呈现该物品的 3D 界面（复用 3D交互制作器的 embed 查看器）。
- **结束物体**：点击物品模型中指定的 `exitMesh` → 触发 `glb-scene-exit` 消息 → 关闭弹层、继续剧情。
- 物品可携带 `interactions`（部位交互）、`sounds`（部位音效）、`defaultView`（默认视角）、`bg`（背景）。

---

## 7. 剧情块系统

剧情由「主剧情 + 分支块」组成，全部存于 localStorage（结构 `{ main: 文本, blocks: { 名称: 文本 } }`）。

- **主剧情（`__MAIN__`）**：始终存在、置顶、不可删除；游戏默认从它开始。
- **分支块**：用户自建，可任意增删改名。
- **跳转**：`<剧情块:块名>` 跳转到该块继续执行。
- **选项**：`<选项:"显示文字",块名>` 弹出选项按钮，点击后跳到对应块。
- **跳回重选**：`<跳回重选>` 返回最近一次选项处重新选择（运行时用返回栈实现）。
- **仓库面板**：剧情块库（`dialogueblock` tab）列出所有块，可：
  - 跳转（插入 `<剧情块:名>`）；
  - 拖拽选项（把块作为选项插入 `<选项:"块名",块名>`）；
  - 重命名（同步更新所有引用）；
  - 删除（主剧情不可删）。

> 剧情块库的卡片（块卡）与二进制素材卡不同，但**必须同样保留删除/重命名/插入指令**能力。

---

## 8. 音乐库「停止音乐」功能块

- 音乐库顶部固定一个**非真实素材**的功能块「停止音乐」。
- 作用：插入 `[停止音乐]` 指令（游戏运行时停止当前背景音乐）。
- 它不是素材，不计入素材总数，渲染时固定置顶、不可删除/重命名。

---

## 9. 存储架构（纯前端）

### 9.1 后端
- **素材二进制 → IndexedDB**（`story-editor` 库，`assets` store，key 形如 `项目id::background:bg1`）。
- **剧情 / 剧情块 / 元数据 → localStorage**（key 带项目 id 后缀，如 `story-editor:story:proj_xxx`）。
- **项目注册表 → localStorage**（`story-editor:projects`）。

### 9.2 项目隔离
- 每个项目有唯一 `id`，作为 IndexedDB key 前缀（`proj_xxx::`）与 localStorage key 后缀（`:proj_xxx`）。
- 切换 / 新建 / 删除项目互不影响。删除项目会清掉其全部剧情与素材。

### 9.3 file:// 原生支持（关键）
- **现代浏览器（Chrome / Edge）在 `file://` 协议下同样支持 IndexedDB**，容量远大于 localStorage。
- 重构后：**`file://` 直接走 IndexedDB，不再强制回退 localStorage，不要求本地服务器。**
- ⚠️ **重要认知**：`file://` 与 `http://localhost` 是**两套完全不同的 IndexedDB 数据库**，互不相通。用户在 A 方式下导入的素材，在 B 方式下看不到——这不是 bug，是浏览器隔离。重构说明文档与界面提示须体现这一点，避免「不显示 = 坏了」的误解。

### 9.4 故障兜底
- IndexedDB 打开 / 读写失败时，**明确报错**（toast / 列表错误态），不再悄悄降级到 localStorage（历史 bug 根源）。
- 私隐模式等极端情况下 IndexedDB 不可用时，提示用户换用常规模式打开。

---

## 10. 库整体导出 / 导入（备份恢复）

- **导出库**：把四库二进制素材（background / item / music / sound）打包成 JSON 下载，用于换部署链接 / 跨设备迁移时**不丢素材**。
- **导入库**：把上述 JSON 写回当前项目（按 lib 逐条保存），返回导入条数。
- 与「场景包导入」（§4.4，仅物品）不同：库导出/导入覆盖全部四类素材。

---

## 11. 重构契约（功能不丢清单）

重构 storage.js / editor.js / index.html / css 时，必须保证以下功能**逐项存在**：

- [x] 五库 tab 切换（背景 / 物品 / 音乐 / 音效 / 剧情块）。
- [x] 背景图导入 + 自动压缩量化（高≤720 / 16 色 / JPG）。
- [x] 物品 GLB 导入（含 exitMesh / interactions / sounds / defaultView / bg 字段）。
- [x] 音频自动归类（<10s 音效 / >30s 音乐 / 10~30s 询问）。
- [x] 场景包导入拆多物品。
- [x] 库面板拖拽落点导入。
- [x] 渐变生成器、噪波生成器（产物存背景库，走 IndexedDB）。
- [x] 素材卡：删除 / 重命名 / 试听 / 横屏拖拽 / 竖屏点按插入指令。
- [x] 物品卡拖入自动附文字提示并使光标定位。
- [x] 音乐库「停止音乐」功能块（置顶、非素材、不可删）。
- [x] 剧情块：主剧情置顶不可删、跳转、选项、跳回重选、仓库面板增删改名（改名同步引用）。
- [x] 库整体导出 / 导入（备份恢复）。
- [x] 项目隔离（命名空间、切换、删除清场）。
- [x] file:// 原生支持 IndexedDB，无服务器提示，无 localStorage 回退。
- [x] **渲染三态**：加载中（骨架/loading）、空（空态插画 + 引导）、错误（明确错误 + 重试）。
- [x] **计数**：每个库顶部「共 N 项」。

> 重构仅做**内部实现整理与健壮性增强**，不删减任何用户可见功能、不改变指令语法、不改变存储 schema 主干。
