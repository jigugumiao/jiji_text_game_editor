# 剧情编辑器 — 测试汇总（2026-07-16）

全套 12 个测试全部通过，无回归。

| 测试文件 | 结果 | 说明 |
|---|---|---|
| block_test.js | ✅ 40/40 | 对话块存储层 + 编辑器解析/API/拖放 + 运行时分支引擎 |
| ai_test.js | ✅ 52/0 | AI 流程（exporter 守卫因 Node 环境跳过，已 `node --check`） |
| ai_selection_test.js | ✅ 24 | AI 选区相关 |
| ai_materials_test.js | ✅ 14 | AI 素材相关 |
| layout_test.js | ✅ 18 | 布局 |
| typewriter_test.js | ✅ PASS | 打字机效果 |
| colorpop_test.js | ✅ 11 | 颜色弹窗（补 Storage 桩） |
| divider_test.js | ✅ 14 | 分隔线（补 Storage 桩） |
| drag_test.js | ✅ 10 | 拖放（补 Storage 桩） |
| settings_test.js | ✅ 32 | 设置（5 tab / 5 AI 模式断言修正） |
| smoke_test.js | ✅ PASS | 运行时模板（`__STORY_DATA__` 中和加 `;`） |
| todo_test.js | ✅ 30/0 | 待办清单（内存版 Storage 桩 + setText 持久化） |

## 本轮修复内容（对话块系统落地后未同步的旧测试桩）
- `smoke_test.js`：`__STORY_DATA__` 中和需带 `;`，否则紧随其后的 IIFE 被解析为 `""(...)` 报 `"" is not a function`。
- `colorpop / divider / settings / drag_test.js`：补 `window.Storage` 的 block 方法桩（noop 安全返回）。
- `settings_test.js`：陈旧断言 `=== 4` → `=== 5`（素材库实际 5 tab 含对话块；AI 菜单实际 5 模式）。
- `todo_test.js`：`computeTodo` 扫描 Storage 块文本，改用内存版 Storage 桩并让 `API.setText` 持久化到 `__MAIN__` 块。

## 关联产品改动
- `exporter.js` `replayTo` 修复：读档后保留 end-card 子节点（避免「完」卡丢失）。
- `index.html` 缓存版本号升到 `v=20260716-39`。

## 后续动作（用户侧）
- 重跑 `node make_exe.js` 让离线 exe 内联最新代码（exporter.js 仍为旧内联）。
